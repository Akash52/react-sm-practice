import React, {FC} from 'react';
import TodoList from '../Components/Todo/TodoList';

const TodoPage: FC = () => {
    return (
        <>
            <TodoList />
        </>
    );
};

export default TodoPage;

import React from 'react';
import {FC} from 'react';
import TodoList from '../Components/Todo/TodoList';

const Home: FC = () => {
    return (
        <>
            <TodoList />
        </>
    );
};

export default Home;

import React, {FC} from 'react';
import Deploy from '../Components/Deploy/Deploy';

const DeployPage: FC = () => {
    return (
        <>
            <Deploy />
        </>
    );
};

export default DeployPage;

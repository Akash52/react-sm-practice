import React, {FC} from 'react';
import ProfileList from '../Components/ProfileList';

const Profile: FC = () => {
    return (
        <>
            <ProfileList />
        </>
    );
};

export default Profile;
